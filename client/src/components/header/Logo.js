import React from 'react'

const styles = {
  logoDiv: {
    width: '20%',
    display: 'flex',
    textAlign: 'center',
    justifyContent: 'flex-start',
    marginRight: 'auto',
    fontSize: '40px',
    maxHeight: ''
  }
}

const Logo = () => (
  <div style={styles.logoDiv}>
    Akshay <br /> Manchanda
  </div>
)

export default Logo
